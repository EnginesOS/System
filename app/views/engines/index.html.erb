<h1>Engines#index</h1>
<p>Find me in app/views/engines/index.html.erb</p>
<table>
	<th>Name</th>
	<th>Hostname</th>
	<th>Set As</th>
	<th>Status</th>
	<th>View</th>
	<th>Actions</th>
<%@engines.each do |engine| 
 state = engine.read_state # faster than querying the docker api 5 times
%>
<tr>	
	<td><%=engine.containerName %></td> 
	<td><%=engine.fqdn %></td>
	<td><%=engine.setState %></td>
	<td><%=state %></td>
	<td>Details</td>
	<% 
		if state == "running"
			%> <td>Stop Pause </td> <%
		else if state == "paused"
			%> <td>Unpause</td> <%
		else if start == "stopped" 
			%> <td>Start / Destroy
			</td> <%
		else if start == "nocontainer" 
			%> <td>Create / Delete </td> 
	<%  end %>
	
</tr>
<% end %>
</table>
